{% extends "base_admin.html.twig" %}

{% block title %}
    {{ 'Commercial.QuoteRequest.View'|trans }}
{% endblock %}

{% block stylesheets %}

{% endblock %}

{% block content_title %}
    {{ 'Commercial.QuoteRequest.QuoteRequests'|trans(null, null, 'DE') }}
{% endblock %}

{% block main_content %}
    <div class="col-md-12">
        <div class="col-md-12">
            <!-- FLASH MESSAGE -->
            <div class="view-box">

                {% for flashMessage in app.session.flashbag.get('error') %}
                    <div class="alert alert-danger">{{ ('Commercial.QuoteRequest.Error.' ~ flashMessage)|trans }}</div>
                {% endfor %}

                {% for flashMessage in app.session.flashbag.get('success') %}
                    <div class="alert alert-success">{{ ('Commercial.QuoteRequest.Success.' ~ flashMessage)|trans }}</div>
                {% endfor %}
            </div>
            <!-- CARD -->
            <div class="card">
                <div class="card-header">
                    <h4>
                        <i class="fa fa-xs fa-quoteRequest"></i> {{ 'Commercial.QuoteRequest.View'|trans }}
                    </h4>
                    <div class="float-right">
                        {% if quoteRequest.postalCode %}
                            <a id="downloadGeneratedQuoteButton"
                               href="{{ path("paprec_quote_request_download", {id: quoteRequest.id}) }}"
                               class="btn btn-sm btn-secondary"><i
                                        class="fa fa-xs fa-download"></i> {{ 'Commercial.QuoteRequest.Download-quote'|trans }}
                            </a>
                        {% endif %}
                        <button class="btn btn-sm btn-primary "
                                data-target="#uploadQuoteRequestFileModal" data-toggle="modal"
                                id="openModalUploadQuoteRequestFile"> {{ 'Commercial.QuoteRequestFile.BtnAddLabel'| trans({}, "messages", app.request.locale) }}
                        </button>
                        <a href="javascript:void(0);"
                           class="btn btn-sm btn-success mr-1 send-generated-contract-button {% if not isAbleToSendContractEmail %}btn-disabled{% endif %}"><i
                                    class="fa fa-xs fa-envelope"></i> {{ 'Commercial.QuoteRequest.Send-contract'|trans }}
                        </a>
                        <a href="{{ path("paprec_quoteRequest_edit", {id: quoteRequest.id}) }}"
                           class="btn btn-sm btn-warning"><i
                                    class="fa fa-xs fa-edit"></i> {{ 'Commercial.QuoteRequest.Edit'|trans }}</a>
                        <a id="commmecialQuoteRequestRemoveButton" href="javascript:void(0);"
                           class="btn btn-sm btn-danger"><i
                                    class="fa fa-xs fa-trash"></i> {{ 'General.Remove'|trans }}</a>

                        <!-- Modal d'ajout de fichiers au mail -->
                        <div class="modal fade" id="uploadQuoteRequestFileModal" tabindex="-1" role="dialog"
                             aria-labelledby="Upload Picture" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered" role="document">
                                <div class="modal-content">
                                    <form id="formAddQuoteRequestFile" name="formAddQuoteRequestFile"
                                          action="{{ path("paprec_quote_request_add_quote_request_file", {quoteRequestId: quoteRequest.id}) }}"
                                          method="post" enctype="multipart/form-data">
                                        <div class="modal-header">
                                            <h5 class="modal-title">
                                                {{ 'Commercial.QuoteRequestFile.AddTitle'| trans({}, "messages", app.request.locale) }}
                                            </h5>
                                            <button type="button" class="close" data-dismiss="modal"
                                                    aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                        </div>
                                        <div class="modal-body">
                                            <div class="form-group">
                                                <div class="form-label">
                                                    {{ 'Commercial.QuoteRequestFile.Path'| trans({}, "messages", app.request.locale) }}
                                                    <span>{{ 'Commercial.QuoteRequestFile.MaxSize'| trans({}, "messages", app.request.locale) }}</span>
                                                </div>
                                                <div class="form-content">
                                                    {{ form_widget(formAddQuoteRequestFile.systemPath, {'attr': {'class': 'form-control'}}) }}
                                                    {{ form_errors(formAddQuoteRequestFile.systemPath) }}
                                                </div>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-sm btn-secondary"
                                                    data-dismiss="modal">
                                                {{ 'General.Cancel'| trans({}, "messages", app.request.locale) }}
                                            </button>
                                            <button type="button" class="btn btn-sm btn-primary"
                                                    id="formAddQuoteRequestFileSubmitButton">
                                                {{ 'General.Save'| trans({}, "messages", app.request.locale) }}
                                            </button>
                                        </div>
                                        {{ form_rest(formAddQuoteRequestFile) }}
                                    </form>
                                </div>
                            </div>
                        </div>

                    </div>
                    <div class="float-left">
                        <a href="{{ path('paprec_quoteRequest_index') }}" class="btn btn-sm btn-default"><i
                                    class="fa fa-xs fa-chevron-left"></i> {{ 'Commercial.QuoteRequest.List'|trans }}</a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="col-md-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title">{{ 'Commercial.QuoteRequest.Informations-FO'|trans }}</h5>
                            </div>
                            <div class="card-body row">
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Type.Type'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.type %} {{ ('Commercial.QuoteRequest.Type.'~ quoteRequest.type|capitalize)|trans }}{% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Origin.Origin'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.origin %} {{ ('Commercial.QuoteRequest.Origin.'~ quoteRequest.origin|capitalize)|trans }}{% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Number'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.number }}
                                        </label>
                                    </div>

                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Locale'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.locale }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.BusinessName'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.businessName }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Civility'|trans }}</label>
                                        {% if quoteRequest.civility is not null %}
                                            <label class="col-form-label">
                                                {{ ('General.' ~ quoteRequest.civility)|trans }}
                                            </label>
                                        {% endif %}
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.LastName'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.lastName }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.FirstName'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.firstName }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Email'|trans }}</label>
                                        <label class="col-form-label">
                                            <a class="send-generated-contract-button" href="javascript:void(0);">
                                                {{ quoteRequest.email }}
                                            </a>
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Phone'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.phone }}
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Public.Contact.Access'| trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.access %}
                                                {{ ('Commercial.AccessList.' ~ quoteRequest.access)|trans }}
                                            {% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Public.Contact.FloorNumber'| trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.floorNumber }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Public.Contact.Staff'| trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.staff %}
                                                {{ ('Commercial.StaffList.' ~ quoteRequest.staff)|trans }}
                                            {% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.PonctualDate'|trans }}</label>
                                        {% if quoteRequest.ponctualDate is not null and quoteRequest.type == 'PONCTUAL' %}
                                            <label class="col-form-label">
                                                {{ quoteRequest.ponctualDate|localizeddate('short', 'none', app.request.locale) }}
                                            </label>
                                        {% endif %}
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.IsMultisite'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ ('General.' ~ (quoteRequest.isMultisite ? 'Yes' : 'No'))|trans }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Address'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.address }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.PostalCode'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.postalCode %}
                                                <a href="{{ path('paprec_postalCode_view', {id: quoteRequest.postalCode.id}) }}">
                                                    {{ quoteRequest.postalCode.code }}
                                                </a>
                                            {% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.City'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.city }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.BillingAddress'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.billingAddress }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.BillingPostalCode'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.billingPostalCode %}
                                                {% if quoteRequest.isSameAddress == 1 %}
                                                    <a href="{{ path('paprec_postalCode_view', {id: quoteRequest.postalCode.id}) }}">
                                                        {{ quoteRequest.billingPostalCode }}
                                                    </a>
                                                {% else %}
                                                    {{ quoteRequest.billingPostalCode }}
                                                {% endif %}
                                            {% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.BillingCity'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.billingCity }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Comment'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.comment }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Public.Catalog.Other-Needs'| trans }}</label>
                                        {% if quoteRequest.otherNeeds and quoteRequest.otherNeeds|length > 0 %}
                                            <ul class="col-form-label">
                                                {% for otherNeed in quoteRequest.otherNeeds %}
                                                    <li class="contact-other-need">- {{ otherNeed.name }}</li>
                                                {% endfor %}
                                            </ul>
                                        {% else %}
                                            <label class="col-form-label">N/A</label>
                                        {% endif %}
                                    </div>
                                </div>
                                <div class="col-md-6 mt-4">
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.SignatoryFirstName1'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.signatoryFirstName1 }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.SignatoryLastName1'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.signatoryLastName1 }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.SignatoryTitle1'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.signatoryTitle1 }}
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-12 mt-3">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title">{{ 'Commercial.QuoteRequest.Informations-BO'|trans }}</h5>
                            </div>
                            <div class="card-body row">
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.QuoteStatus'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ ('Commercial.QuoteStatusList.' ~ quoteRequest.quoteStatus)|trans }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.RegularDuration'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.duration is null %}
                                                0 {{ 'Commercial.QuoteRequest.month'|trans }}
                                            {% else %}
                                                {{ quoteRequest.duration }} {{ 'Commercial.QuoteRequest.month'|trans }}
                                            {% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.StartDate'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.startDate is not null %}
                                                {{ quoteRequest.startDate|localizeddate('short', 'none', app.request.locale) }}
                                            {% endif %}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.CustomerId'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.customerId }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.UserInCharge'|trans }}</label>
                                        <label class="col-form-label">
                                            {% if quoteRequest.userInCharge %}
                                                {% if quoteRequest.userInCharge.deleted is null %}
                                                    <a href="{{ path('paprec_user_view', {id: quoteRequest.userInCharge.id}) }}">
                                                        {{ quoteRequest.userInCharge.firstName }} {{ quoteRequest.userInCharge.lastName }}
                                                    </a>
                                                {% else %}
                                                    {{ quoteRequest.userInCharge.firstName }} {{ quoteRequest.userInCharge.lastName }}
                                                {% endif %}
                                                {% if quoteRequest.userInCharge.deleted is not null %}
                                                    {{ 'Catalog.PostalCode.DeletedUser'|trans }}
                                                {% elseif not quoteRequest.userInCharge.enabled %}
                                                    {{ 'Catalog.PostalCode.DisabledUser'|trans }}
                                                {% endif %}
                                            {% endif %}
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.Reference'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.reference }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.TotalAmount'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.totalAmount|formatAmount('EUR', app.request.locale) }}
                                        </label>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.OverallDiscount'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }}
                                        </label>
                                    </div>
                                    {# <div class="form-group row"> #}
                                    {# <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.AnnualBudget'|trans }}</label> #}
                                    {# <label class="col-form-label"> #}
                                    {# {{ quoteRequest.annualBudget|formatAmount('EUR', app.request.locale) }} #}
                                    {# </label> #}
                                    {# </div> #}
                                    <div class="form-group row">
                                        <label class="col-md-4 col-form-label">{{ 'Commercial.QuoteRequest.SalesmanComment'|trans }}</label>
                                        <label class="col-form-label">
                                            {{ quoteRequest.salesmanComment }}
                                        </label>
                                    </div>

                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-12 mt-3">
                        <div class="card">
                            <div class="card-header">
                                <h5>
                                    {{ 'Commercial.QuoteRequest.ProductsList'|trans }}
                                </h5>
                                <div class="float-right">

                                    <a href="{{ path('paprec_quoteRequest_addLine', {id: quoteRequest.id}) }}"
                                       class="btn btn-sm btn-outline-primary">
                                        <i class="fa fa-xs fa-plus"></i> {{ 'Commercial.QuoteRequest.AddLine'|trans }}
                                    </a>
                                </div>
                            </div>
                            <div class="card-body">
                                <table id="quoteRequestLinesDatatable"
                                       class="table table-striped table-bordered table-hover"
                                       cellpadding="0" cellspacing="0" border="0">
                                    <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.ProductName'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.Quantity'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.RentalPrice'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.Transport'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.PassingFrequency'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.Treatment'|trans }} </th>
                                        <th>{{ 'Commercial.QuoteRequestLine.Traceability'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.TreatmentCollect'|trans }}</th>
                                        <th>{{ 'Commercial.QuoteRequestLine.TotalAmount'|trans }}</th>

                                        <th></th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% set color = null %}
                                    {% if quoteRequest.overallDiscount == 0 %}
                                        {% set color = 'black' %}
                                    {% elseif quoteRequest.overallDiscount > 0 %}
                                        {% set color = 'green' %}
                                    {% elseif quoteRequest.overallDiscount < 0 %}
                                        {% set color = 'red' %}
                                    {% endif %}
                                    {% for quoteRequestLine in quoteRequest.quoteRequestLines %}
                                        <tr>
                                            <td>{{ quoteRequestLine.id|formatId(6) }}</td>
                                            <td>
                                                <a href="{{ path('paprec_product_view', {id: quoteRequestLine.product.id}) }}">
                                                    {{ quoteRequestLine.product|productLabelTranslation(app.user.lang) }}
                                                </a>
                                            </td>
                                            <td>{{ quoteRequestLine.quantity }}</td>
                                            <td>
                                                {% if quoteRequestLine.editableRentalUnitPrice == 0 %}
                                                    0
                                                {% else %}
                                                    {{ (((quoteRequestLine.rentalRate / 1000000000000000) * (quoteRequestLine.editableRentalUnitPrice)))|formatAmount('EUR', app.request.locale) }}
{#                                                    #}{# <span class="small color-darkgray">({{ ((quoteRequestLine.editableRentalUnitPrice - quoteRequestLine.rentalUnitPrice) / quoteRequestLine.rentalUnitPrice * 100 * 100)|formatAmount('PERCENTAGE', app.request.locale) }})</span> #}
{#                                                    {% set percentageRental = (quoteRequest.overallDiscount + (quoteRequestLine.editableRentalUnitPrice - quoteRequestLine.rentalUnitPrice) / quoteRequestLine.rentalUnitPrice * 100 * 100) %}#}
{#                                                    {% set colorRental = null %}#}
{#                                                    {% if percentageRental == 0 %}#}
{#                                                        {% set colorRental = 'black' %}#}
{#                                                    {% elseif percentageRental > 0 %}#}
{#                                                        {% set colorRental = 'green' %}#}
{#                                                    {% elseif percentageRental < 0 %}#}
{#                                                        {% set colorRental = 'red' %}#}
{#                                                    {% endif %}#}
                                                    <span class="small"
                                                          style="color: {{ color }}">({{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }})</span>
                                                {% endif %}
                                            </td>
                                            <td>
                                                {% if quoteRequestLine.editableTransportUnitPrice == 0 %}
                                                    0
                                                {% else %}
                                                    {{ (((quoteRequestLine.transportRate / 1000000000000000) * (quoteRequestLine.editableTransportUnitPrice)))|formatAmount('EUR', app.request.locale) }}
{#                                                    {% set percentageTransport = (quoteRequest.overallDiscount + (quoteRequestLine.editableTransportUnitPrice - quoteRequestLine.transportUnitPrice) / quoteRequestLine.transportUnitPrice * 100 * 100) %}#}
{#                                                    {% set colorTransport = null %}#}
{#                                                    {% if percentageTransport == 0 %}#}
{#                                                        {% set colorTransport = 'black' %}#}
{#                                                    {% elseif percentageTransport > 0 %}#}
{#                                                        {% set colorTransport = 'green' %}#}
{#                                                    {% elseif percentageTransport < 0 %}#}
{#                                                        {% set colorTransport = 'red' %}#}
{#                                                    {% endif %}#}
{#                                                    <span class="small"#}
{#                                                          style="color: {{ colorTransport }}">({{ percentageTransport|formatAmount('PERCENTAGE', app.request.locale) }})</span>#}
                                                    <span class="small"
                                                          style="color: {{ color }}">({{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }})</span>
                                                {% endif %}
                                            </td>
                                            <td>
                                                {% if quoteRequestLine.frequency == 'unknown' %}
                                                    {{ 'General.Frequency.Unknown'|trans({}, "messages", app.request.locale) }}
                                                {% else %}
                                                    {% set frequencyIntervalName = 'General.Frequency.' ~ quoteRequestLine.frequencyInterval|capitalize %}
                                                    {{ quoteRequestLine.frequencyTimes }}
                                                    x/ {{ frequencyIntervalName|trans({}, "messages", app.request.locale) }}
                                                {% endif %}
                                            </td>
                                            <td>
                                                {% if quoteRequestLine.editableTreatmentUnitPrice == 0 %}
                                                    0
                                                {% else %}
                                                    {{ (((quoteRequestLine.treatmentRate / 1000000000000000) * (quoteRequestLine.editableTreatmentUnitPrice)))|formatAmount('EUR', app.request.locale) }}
{#                                                    {% set percentageTreatment = (quoteRequest.overallDiscount + (quoteRequestLine.editableTreatmentUnitPrice - quoteRequestLine.treatmentUnitPrice) / quoteRequestLine.treatmentUnitPrice * 100 * 100) %}#}
{#                                                    {% set colorTreatment = null %}#}
{#                                                    {% if percentageTreatment == 0 %}#}
{#                                                        {% set colorTreatment = 'black' %}#}
{#                                                    {% elseif percentageTreatment > 0 %}#}
{#                                                        {% set colorTreatment = 'green' %}#}
{#                                                    {% elseif percentageTreatment < 0 %}#}
{#                                                        {% set colorTreatment = 'red' %}#}
{#                                                    {% endif %}#}
{#                                                    <span class="small"#}
{#                                                          style="color: {{ colorTreatment }}">#}
{#                                                    ({{ percentageTreatment|formatAmount('PERCENTAGE', app.request.locale) }})#}
{#                                                    </span>#}
                                                    {# <span class="small color-darkgray">({{ ((quoteRequestLine.editableTreatmentUnitPrice - quoteRequestLine.treatmentUnitPrice) / quoteRequestLine.treatmentUnitPrice * 100 * 100)|formatAmount('PERCENTAGE', app.request.locale) }})</span> #}
                                                    <span class="small"
                                                          style="color: {{ color }}">({{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }})</span>
                                                {% endif %}
                                            </td>
                                            <td>
                                                {% if quoteRequestLine.editableTraceabilityUnitPrice == 0 %}
                                                    0
                                                {% else %}
                                                    {{ (((quoteRequestLine.traceabilityRate / 1000000000000000) * (quoteRequestLine.editableTraceabilityUnitPrice)))|formatAmount('EUR', app.request.locale) }}
{#                                                    {% set percentageTreatment = (quoteRequest.overallDiscount + (quoteRequestLine.editableTreatmentUnitPrice - quoteRequestLine.treatmentUnitPrice) / quoteRequestLine.treatmentUnitPrice * 100 * 100) %}#}
{#                                                    {% set percentageTraceability = (quoteRequest.overallDiscount + (quoteRequestLine.editableTraceabilityUnitPrice - quoteRequestLine.traceabilityUnitPrice) / quoteRequestLine.traceabilityUnitPrice * 100 * 100) %}#}
{#                                                    {% set colorTraceability = null %}#}
{#                                                    {% if percentageTraceability == 0 %}#}
{#                                                        {% set colorTraceability = 'black' %}#}
{#                                                    {% elseif percentageTraceability > 0 %}#}
{#                                                        {% set colorTraceability = 'green' %}#}
{#                                                    {% elseif percentageTraceability < 0 %}#}
{#                                                        {% set colorTraceability = 'red' %}#}
{#                                                    {% endif %}#}
{#                                                    <span class="small"#}
{#                                                          style="color: {{ colorTraceability }}">({{ percentageTraceability|formatAmount('PERCENTAGE', app.request.locale) }})</span>#}
                                                    {# <span class="small color-darkgray">({{ ((quoteRequestLine.editableTraceabilityUnitPrice - quoteRequestLine.traceabilityUnitPrice) / quoteRequestLine.traceabilityUnitPrice * 100 * 100)|formatAmount('PERCENTAGE', app.request.locale) }})</span> #}
                                                    <span class="small"
                                                          style="color: {{ color }}">({{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }})</span>
                                                {% endif %}
                                            </td>

{#                                            {% if quoteRequest.overallDiscount == 0 %}#}
{#                                                {% set colorOverallDiscount = 'black' %}#}
{#                                            {% elseif quoteRequest.overallDiscount > 0 %}#}
{#                                                {% set colorOverallDiscount = 'green' %}#}
{#                                            {% elseif quoteRequest.overallDiscount < 0 %}#}
{#                                                {% set colorOverallDiscount = 'red' %}#}
{#                                            {% endif %}#}

                                            <td>{{ quoteRequestLine.treatmentCollectPrice|formatAmount('EUR', app.request.locale) }}
                                                <span class="small"
                                                      style="color: {{ color }}">({{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }})</span>
                                            </td>
                                            <td>{{ quoteRequestLine.totalAmount|formatAmount('EUR', app.request.locale) }}
                                                <span class="small"
                                                      style="color: {{ color }}">({{ quoteRequest.overallDiscount|formatAmount('PERCENTAGE', app.request.locale) }})</span>
                                            </td>
                                            <td class="text-center">
                                                <a href="{{ path('paprec_quoteRequest_editLine', {id: quoteRequest.id, quoteLineId: quoteRequestLine.id}) }}"
                                                   class="btn btn-sm btn-warning btn-sm mx-1"
                                                   data-id="{{ quoteRequestLine.id }}">
                                                    <i class="fa fa-xs fa-edit"></i>
                                                </a>
                                                <a href="#"
                                                   class="btn btn-sm btn-danger btn-sm commercialQuoteRequestLineRemoveButton"
                                                   data-id="{{ quoteRequestLine.id }}">
                                                    <i class="fa fa-xs fa-trash"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-12 mt-3">
                        <div class="card">
                            <div class="card-header">
                                <h5>
                                    {{ 'Commercial.QuoteRequest.ProductsList'|trans }}
                                </h5>
                                <div class="float-right">

                                    <a href="{{ path('paprec_quoteRequest_addLine', {id: quoteRequest.id}) }}"
                                       class="btn btn-sm btn-outline-primary">
                                        <i class="fa fa-xs fa-plus"></i> {{ 'Commercial.QuoteRequest.AddLine'|trans }}
                                    </a>
                                </div>
                            </div>
                            <div class="card-body">
                                <table id="quoteRequestFileListDatatable"
                                       class="table table-striped table-bordered table-hover"
                                       cellpadding="0" cellspacing="0" border="0">
                                    <thead>
                                    <tr>
                                        <th>{{ 'Commercial.QuoteRequestFile.FileNameColumn'| trans({}, "messages", app.request.locale) }}</th>
                                        <th>{{ 'Commercial.QuoteRequestFile.FileTypeColumn'| trans({}, "messages", app.request.locale) }}</th>
                                        <th></th>
                                    </tr>
                                    </thead>
                                    <tbody>

                                    {% if quoteRequest.quoteRequestFiles is not null and quoteRequest.quoteRequestFiles|length > 0 %}
                                        {% for quoteRequestFile in quoteRequest.quoteRequestFiles %}

                                            <tr>
                                                <td>
                                                    {{ quoteRequestFile.originalFileName }}
                                                </td>

                                                <td>
                                                </td>

                                                <td class="text-center">
                                                    <a href="#"
                                                       class="btn btn-sm btn-danger btn-sm commercialQuoteRequestFileRemoveButton"
                                                       data-id="{{ quoteRequestFile.id }}">
                                                        <i class="fa fa-xs fa-trash"></i>
                                                    </a>
                                                </td>
                                            </tr>

                                        {% endfor %}
                                    {% endif %}

                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-12 mt-3">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title">{{ 'General.System-information'|trans }}</h5>
                            </div>
                            <div class="card-body">

                                <div class="form-group row">
                                    <label class="col-md-4 col-form-label">{{ 'General.Date-creation'|trans }}</label>
                                    <label class="col-form-label">
                                        {{ quoteRequest.dateCreation|localizeddate('short', 'medium', app.request.locale) }}
                                    </label>
                                </div>

                                <div class="form-group row">
                                    <label class="col-md-4 col-form-label">{{ 'General.Date-update'|trans }}</label>
                                    <label class="col-form-label">
                                        {{ quoteRequest.dateUpdate is empty ? "" : quoteRequest.dateUpdate|localizeddate('short', 'medium', app.request.locale) }}
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $(function () {

            $('#commmecialQuoteRequestRemoveButton').on('click', function () {

                {% if is_granted('ROLE_ADMIN') %}

                var url = "{{ path("paprec_quoteRequest_remove", {id: quoteRequest.id}) }}";

                Swal.fire({
                    title: "{{ 'General.Confirm'|trans }}",
                    text: "{{ 'General.Confirm-remove'|trans }}",
                    showCancelButton: true,
                    confirmButtonText: "{{ 'General.Yes'|trans }}",
                    cancelButtonText: "{{ 'General.No'|trans }}"
                }).then(function (result) {
                    if (result.value) {
                        $(location).attr('href', url);
                    }
                });
                {% else %}
                Swal.fire({
                    title: "{{ 'General.CantRemove'|trans }}",
                    text: "",
                    showCancelButton: false,
                    confirmButtonText: "{{ 'General.Cancel'|trans }}"
                });
                {% endif %}


            });
            $('.commercialQuoteRequestLineRemoveButton').on('click', function () {
                quoteLineId = $(this).data('id');
                var url = "{{ path("paprec_quoteRequest_removeLine", {id: quoteRequest.id, quoteLineId: 'quoteLineIdTmp'}) }}".replace('quoteLineIdTmp', quoteLineId);


                Swal.fire({
                    title: "{{ 'General.Confirm'|trans }}",
                    text: "{{ 'General.Confirm-remove'|trans }}",
                    showCancelButton: true,
                    confirmButtonText: "{{ 'General.Yes'|trans }}",
                    cancelButtonText: "{{ 'General.No'|trans }}"
                }).then(function (result) {
                    if (result.value) {
                        $(location).attr('href', url);
                    }
                });

            });

            $('.send-generated-contract-button').on('click', function () {
                if (!$(this).hasClass('btn-disabled')) {
                    var url = "{{ path("paprec_quoteRequest_sendGeneratedContract", {id: quoteRequest.id}) }}";

                    Swal.fire({
                        title: "{{ 'General.Confirm'|trans }}",
                        text: "{{ 'Commercial.QuoteRequest.Confirm-send-contract'|trans }}",
                        showCancelButton: true,
                        confirmButtonText: "{{ 'General.Yes'|trans }}",
                        cancelButtonText: "{{ 'General.No'|trans }}"
                    }).then(function (result) {
                        if (result.value) {
                            $(location).attr('href', url);
                        }
                    });
                }
            });

            $('#formAddQuoteRequestFileSubmitButton').on('click', function () {
                $('#formAddQuoteRequestFile').submit();
            });

            $('.commercialQuoteRequestFileRemoveButton').on('click', function () {
                var quoteRequestFileId = $(this).data('id');
                var url = "{{ path("paprec_quote_request_remove_quote_request_file", {quoteRequestId: quoteRequest.id, quoteRequestFileId: '0000'}) }}".replace('0000', quoteRequestFileId);

                Swal.fire({
                    title: "{{ 'General.Confirm'|trans }}",
                    text: "{{ 'General.Confirm-remove'|trans }}",
                    showCancelButton: true,
                    confirmButtonText: "{{ 'General.Yes'|trans }}",
                    cancelButtonText: "{{ 'General.No'|trans }}"
                }).then(function (result) {
                    if (result.value) {
                        $(location).attr('href', url);
                    }
                });

            });

        });
    </script>
{% endblock %}
