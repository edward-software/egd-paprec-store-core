{% extends "base_admin.html.twig" %}

{% block title %}
    {{ 'Catalog.MissionSheetProductLabel.Add'|trans }}
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block content_title %}
    {{ 'Catalog.MissionSheetProductLabel.Add'|trans }}
{% endblock %}

{% block main_content %}
    <form id="catalogMissionSheetProductForm" action="" method="post">

        <div class="col-md-12">
            <!-- BOX -->
            <div class="card">
                <div class="card-header">
                    <h4>
                        <i class="fa fa-xs fa-plus"></i> {{ 'Catalog.MissionSheetProductLabel.Add'|trans }}
                    </h4>
                    <div class="float-right">
                        <a href="javascript:void(0);"
                           class="btn btn-sm btn-success catalog-mission-sheet-product-mission-sheet-product-label-save-button"><i
                                    class="fa fa-xs fa-check"></i> {{ 'General.Save'|trans }}</a>
                        <a href="javascript:void(0);"
                           class="btn btn-sm btn-danger catalog-mission-sheet-product-mission-sheet-product-label-cancel-button"><i
                                    class="fa fa-xs fa-close"></i> {{ 'General.Cancel'|trans }}</a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="col-md-7">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title">{{ 'General.Information'|trans }}</h5>
                            </div>
                            <div class="card-body">
                                <div class="form-group">
                                    <div class="form-label">{{ 'Catalog.MissionSheetProductLabel.Language.Language'|trans }}</div>
                                    <div class="form-content">
                                        {{ form_widget(form.language, {'attr': {'class': 'form-control missionSheetProduct-label-language-input'}}) }}
                                        {{ form_errors(form.language) }}
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="form-label">{{ 'Catalog.MissionSheetProductLabel.Name'|trans }}</div>
                                    <div class="form-content">
                                        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                                        {{ form_errors(form.name) }}
                                    </div>
                                </div>

                                <div class="form-group">
                                    <div class="form-label">{{ 'Catalog.MissionSheetProductLabel.Short-description'|trans }}</div>
                                    <div class="form-content">
                                        {{ form_widget(form.shortDescription, {'attr': {'class': 'form-control'}}) }}
                                        {{ form_errors(form.shortDescription) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card-footer">
                    <div class="float-right">
                        <a href="javascript:void(0);"
                           class="btn btn-sm btn-success catalog-mission-sheet-product-mission-sheet-product-label-save-button"><i
                                    class="fa fa-xs fa-check"></i> {{ 'General.Save'|trans }}</a>
                        <a href="javascript:void(0);"
                           class="btn btn-sm btn-danger catalog-mission-sheet-product-mission-sheet-product-label-cancel-button"><i
                                    class="fa fa-xs fa-close"></i> {{ 'General.Cancel'|trans }}</a>
                    </div>
                </div>
            </div>
        </div>
        {{ form_rest(form) }}
    </form>



{% endblock %}

{% block javascripts %}

    <script>
        $(function () {

            $('.catalog-mission-sheet-product-mission-sheet-product-label-save-button').on('click', function () {
                $('#catalogMissionSheetProductForm').submit();
            });

            $('.catalog-mission-sheet-product-mission-sheet-product-label-cancel-button').on('click', function () {

                var url = "{{ path("paprec_mission_sheet_product_view", {id: missionSheetProduct.id}) }}";

                Swal.fire({
                    title: "{{ 'General.Confirm'|trans }}",
                    text: "{{ 'General.Confirm-cancel'|trans }}",
                    showCancelButton: true,
                    confirmButtonText: "{{ 'General.Yes'|trans }}",
                    cancelButtonText: "{{ 'General.No'|trans }}"
                }).then(function (result) {
                    if (result.value) {
                        $(location).attr('href', url);
                    }
                });

            });

        });
    </script>
{% endblock %}
